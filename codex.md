## Regras para o Codex

- Não executar `flutter run`.
- Não executar `flutter test` automaticamente.
- Para validação rápida, usar apenas `flutter analyze`.
- Se precisar testar, sugerir o comando ao usuário em vez de executar.
